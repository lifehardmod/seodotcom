import MainNav from "./MainNav";

const Header = () => {
  return (
    <div className="flex items-center justify-center min-h-[80px] border-b-[0.3px] border-black">
      <MainNav />
    </div>
  );
};

export default Header;
